<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html xmlns:th="http://www.thymeleaf.org">
<head>
<link rel="stylesheet" type="text/css"
	href="/resources/style/styles.css" />
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Forum</title>
</head>
<body>

	<h1 align="center" th:text="#{forum}" />
	<br />
	<input type="submit" th:value="#{add_post}" id=addpost_button
		onclick="window.location='/add/post'">

	<br />
	
	<table id="table" th:each="post : ${posts}">

		<tr>
			<th th:text="${post.subject}" id="th1"/>
		</tr>
		<tr>
			<td th:text="${post.text}" />
		</tr>
		<div th:if="${post.comments.size() != 0}">
		<tr>
		<table id="table1" th:each="comments, iter : ${post.comments}">
		<tr>
		<th th:text="#{comments} + '&nbsp' + ${iter.index+1}" />
		</tr>
		<tr>
		<td th:text="${post.comments.get(iter.index)}" />
		</tr>
		<br/>
		</table>
		</tr>
		</div>
		<tr>
	<table>
		<tr>
		<td><a th:href="@{'post/' + ${post.id} + '/comment'}"><input type="button" th:value="#{see_comments}"/></a></td>
		<td><a th:href="@{'/add/' + ${post.id} + '/comment'}"><input type="button" th:value="#{add_comment}"/></a></td>
		</tr>
		
	</table>
	
	<br />
	<br />
	
	</table>

</body>
</html>